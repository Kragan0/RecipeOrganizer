import { RecipePipePipe } from './recipe-pipe.pipe';

describe('RecipePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipePipePipe();
    expect(pipe).toBeTruthy();
  });
});
